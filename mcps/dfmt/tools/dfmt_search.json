{
  "name": "dfmt_search",
  "description": "Search session events",
  "inputSchema": {
    "properties": {
      "limit": {
        "description": "Maximum results",
        "type": "integer"
      },
      "query": {
        "description": "Search query",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "type": "object"
  }
}