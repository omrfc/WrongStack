feat(tui): AutoPhase PhaseMonitor state and event wiring

State: autoPhase branch added to State type (title, phases, runningPhaseIds,
elapsedMs, monitorOpen). Initial state null.

Reducer: 5 new actions — autoPhaseInit, autoPhasePhaseUpdate,
autoPhaseRunningPhases, autoPhaseElapsed, autoPhaseMonitorToggle,
autoPhaseReset.

AppProps: subscribeAutoPhase prop for wiring PhaseOrchestrator events.

Effect: subscribes to phase.started/completed/failed/statusChange/
taskCompleted/autonomous.tick/graph.completed/graph.failed and
dispatches corresponding reducer actions.

Ctrl+P: toggles the PhaseMonitor overlay via handleKey.